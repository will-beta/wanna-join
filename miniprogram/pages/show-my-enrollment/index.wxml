<view wx:if="{{!userInfo}}">
  <login bind:getUserInfo="onGetUserInfo"></login>
</view>
<view wx:else>
  <show-my-enrollment user-info="{{userInfo}}" activity-id="{{activityId}}"></show-my-enrollment>
</view>